# The domain (no protocol, no port) that the WebAuthn RP runs on.
# Use "localhost" for local development.
RP_ID=localhost

# The exact origin the browser will send requests from (Vite dev server).
ORIGIN=http://localhost:5174

# Port for the Express server
PORT=3001
